<%= provide(:title, "index") %>
<%
# reactで書いた部品を使いたい時は react_component で呼び出す。
# react_componentはhelperで、react-railsをインストールした時に自動的に生成してくれる。
# (どこに定義されているのかは不明...)
%>

<%= react_component("CommentBox", url: "/api/v1/comments.json", pollInterval: 2000) %>
